sudo: required
language: java
dist: bionic
jdk: openjdk8
python:
  - 3.8
  
addons:
  sonarcloud:
    organization: "oktayalizada"
    token: $SONAR_TOKEN

script:
  - mvn clean verify sonar:sonar -Pcoverage 
